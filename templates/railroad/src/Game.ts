export class Game {
  private readonly contract = {
    width: 1080,
    height: 1920,
  };

  constructor(_canvasId: string) {
    void _canvasId;
    void this.contract;
  }

  async init(): Promise<void> {
    return Promise.resolve();
  }
}
